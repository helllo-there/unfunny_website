document.addEventListener('keydown', function(event) {
    // Check if Shift, Alt, and C are pressed simultaneously
    if (event.key === 'c') {
        // Redirect to site
        window.location.href = 'https://cornhub.website/';
    }
});